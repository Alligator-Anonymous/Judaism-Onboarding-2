import Dexie from "dexie";

// Placeholder seed script. In a production build this would populate IndexedDB with starter data.
// For now we simply log to console to indicate seed path.
(async () => {
  // eslint-disable-next-line no-console
  console.log("Seed data would sync to IndexedDB here.");
})();
