import React from "react";
import App from "./App";

// Placeholder for future multi-window routing. Currently just exports the root component.
export const routes = [
  {
    path: "/",
    element: <App />
  }
];
